#include <stdio.h>

int main() {
    // this is the first problem, I am finding the area of a rectangle
    int a = 2;
    int b = 3;
    int area = a*b;
    printf("The area of the rectangle with sides %d and %d is: %d\n", a, b, area);
    return 0;
}